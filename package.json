{
  "name": "daid-anci",
  "version": "0.1.0",
  "private": true,
  "description": "AAACT Accessibility Digital Design Team bilingual website",
  "scripts": {
    "dev": "eleventy --serve --input=src --output=dist",
    "build": "eleventy --input=src --output=dist"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
